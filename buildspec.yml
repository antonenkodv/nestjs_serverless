version: 0.2

phases:
  install:
    commands:
      - npm i
      - npm i -g serverless
      - pip install --upgrade pip
      - pip install --upgrade awscli
      - npm test
  build:
    commands
      - sls deploy -s dev
#artifacts:
#  files:
#    - '**/*'
#  discard-paths: no

